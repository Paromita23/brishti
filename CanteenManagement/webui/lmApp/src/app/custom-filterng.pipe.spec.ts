import { CustomFilterngPipe } from './custom-filterng.pipe';

describe('CustomFilterngPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomFilterngPipe();
    expect(pipe).toBeTruthy();
  });
});
